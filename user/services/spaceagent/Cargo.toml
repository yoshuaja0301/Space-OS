[package]
name = "spaceagent"
description = "Agent runtime: reads, patches and checks a workspace through the Tool Broker (G01)"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[[bin]]
name = "spaceagent"
test = false
bench = false

[dependencies]
spaceabi.workspace = true
libspace.workspace = true
