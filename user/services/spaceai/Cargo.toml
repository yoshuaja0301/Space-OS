[package]
name = "spaceai"
description = "Space OS AI runtime: loads a SpaceLM model and generates tokens through the Compute ABI"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[[bin]]
name = "spaceai"
test = false
bench = false

[dependencies]
spaceabi.workspace = true
libspace.workspace = true
