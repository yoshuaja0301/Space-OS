[package]
name = "fault"
description = "Test: touches forbidden memory and must be killed by the kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[[bin]]
name = "fault"
test = false
bench = false

[dependencies]
spaceabi.workspace = true
libspace.workspace = true
