[package]
name = "spin"
description = "Test: runaway loop that must be preempted and killed"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[[bin]]
name = "spin"
test = false
bench = false

[dependencies]
spaceabi.workspace = true
libspace.workspace = true
