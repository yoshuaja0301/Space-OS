[package]
name = "blocker"
description = "Test: blocks forever in sleep, wait or recv so the parent can kill it mid-block"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[[bin]]
name = "blocker"
test = false
bench = false

[dependencies]
spaceabi.workspace = true
libspace.workspace = true
