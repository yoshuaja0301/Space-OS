[package]
name = "init"
description = "Space OS init: first user process, spawns services and runs the acceptance tests"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[[bin]]
name = "init"
test = false
bench = false

[dependencies]
spaceabi.workspace = true
libspace.workspace = true
